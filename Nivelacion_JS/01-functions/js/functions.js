const resta = (a,b) => a - b;
const sumar = (a, b) => {
    return a + b;
}

const resultado1 = sumar(5,3);
document.write("<p>La respuesta de la suma es = "+resultado1+"</p>");

const resultado2 = resta(5,3);
document.write("<p>La respuesta de la resta es = "+resultado2+"</p>");